# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "shouldbee/ubuntu-14.04-with-docker"
  config.vm.box_version = "0.5"
  config.vm.network "private_network", ip: "192.168.0.55"
end
